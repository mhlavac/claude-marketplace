# {{NAME_TITLE}} — Critical Facts

**Budget: ≤200 tokens (~140 words).** Mirrored into Tier 2 `MEMORY.md` under
`<critical_facts>`. Loaded on EVERY spawn before any other reasoning.

**Edit only when a fact materially changes.** Frequent edits = drift = the
facts aren't critical.

---

<!-- TODO: 4–8 lines max. The most load-bearing facts about who/what this
     agent serves. Examples (delete and replace — these are deliberately
     heterogeneous so the reader doesn't anchor on one persona):

- User / scope: <who or what this agent works on>
- Domain: <what this agent covers>
- Current phase: <release cycle, sprint demo, mid-cycle review, ...>
- One immovable constraint: <budget, latency budget, headcount, ...>
- One active commitment: <project/decision in flight>
- Default refusal: <one thing the agent never does, restated>

-->
